# 「我创造的完美球员」游玩体验改动方案

> 版本: v1.0 · 面向对象:纯前端生涯模拟游戏
> 源码位置: `__ai_app.html`(主逻辑,约 2.3 万行)、`game-config.js`(平衡参数)、`index.html`(入口壳)
> 原则:**改动全部做成"可开关/可回退"**,默认值尊重作者已调校的平衡,不破坏原版体验。

---

## 一、当前功能与代码结构总览

| 模块 | 主要函数/参数 | 位置 |
|---|---|---|
| 建球员(老虎机夺属性) | `renderBuildUI`、`renderTeamPicker` | `__ai_app.html` |
| 属性/总评平衡 | `SIM_CONFIG.BUILD`、`OVR_WEIGHTS` | `game-config.js` |
| 赛季模拟(逐场/快进) | `quickSimAllGames`、`simNextWithDelay` | `__ai_app.html:5601` |
| 手动逐场/批处理 | `simNextGame`、`simBatch(10)`(已标记[实验性]) | `__ai_app.html:9034` |
| 季后赛 | `simPlayoffSeries`、`buildPlayoffBracket` | `__ai_app.html` |
| 训练营加点 | `renderTrainingCamp`、`renderTrainingAttrs` | `__ai_app.html:14468` |
| 经济/商店 | `renderPlayerShop`、`renderPlayerShopModal` | `__ai_app.html:16252` |
| 奖项评选 | `SIM_CONFIG.AWARDS`、`AWARD_RULES` | `game-config.js` |
| 阵容/轮换 | `calcTeamLineup` | `__ai_app.html` |
| 存档 | localStorage 自动存档 + 手动快照/档案库 | `__ai_app.html:22224` |
| 难度/球队加成 | `SIM_BALANCE.playerBoostByOvr` | `game-config.js:552` |

## 二、拟改动清单(按你勾选的 4 个方向)

### A. 操作便利性(低风险,默认即生效)

1. **一键模拟整赛季(含季后赛)**
   现状:常规赛已有一键快进 `quickSimAllGames`,但**季后赛**后段仍需逐场/手动推进,且快速模拟中无法暂停。
   改动:在赛季页加「🏁 一键模拟至赛季结束」按钮,自动串联:剩余常规赛 → 完整季后赛 → 进入休赛期;中间在发生事件、伤病、关键时刻、决胜球时**自动暂停等待确认**,其余自动推进。保留原有手动逐场方式。

2. **模拟控件显性化**
   现状:赛季页仍保留「进行下一场 / ⏩ 进行下十场」按钮(已标记实验性),与新快进逻辑并存,入口分散。
   改动:统一为「⏩ 快进整季」与「单场 / 十场」三档,并在顶部显示模拟进度百分比(如 `12/82 · 73%`),避免"82 场真的跑完了没"的疑惑。

3. **常用操作快捷面板**
   现状:训练营、商店、交易、自由市场入口分散在休赛期/弹窗。
   改动:赛季页右上角加「🎛 快捷面板」,一键直达 商店 / 我的卡片 / 训练营说明 / 规则帮助,减少来回点击。

### B. 难度与平衡(做成「难度档位」开关,默认保持原版)

4. **新增三档难度:周目休闲 / 标准(默认) / 挑战**
   - 在开始游戏页加难度选择,写入存档。
   - 生效位置(全部做成乘数,改一行即可):
     - `SIM_BALANCE.playerBoostByOvr` → 玩家球队净效率加成(休闲×1.3、标准×1.0、挑战×0.75)
     - `SIM_BALANCE.winMax`/`regularWinMax`/`playoffWinMax` → 胜率上限
     - `AWARD_RULES.MVP.userGrowthBonus` → MVP 竞争强度
     - `PLAYER_STATS.SEASON_FORM_DIST` → 低迷/爆发年占比(休闲减少低迷年)
   - 难度只影响模拟参数,**不锁定任何功能**,随时可切。

5. **「挑战模式」增强 AI**
   - 提高 `npcCareerYear.chance`(NPC 数据爆发概率)
   - 提高 `npcInjury`/`npcRest` 触发与惩罚,NPC 球星更"强"
   - 关闭玩家隐形加成(heroBonus 归 1.0)

### C. 新功能 / 新玩法

6. **更多历史时代**
   现状:1984 / 1996 / 2003 三档。
   改动:追加 **2009(库里/哈登/格里芬黄金一代)** 与 **2016(字母哥/约基奇一代)** 两个可选开局时代(数据从 `historical-players.js` / `era-*.js` 结构复制新增),每种含对应名单、工资帽档位与色调皮肤。

7. **「传奇图鉴 / 成就」汇总页**
   现状:生涯终点有大海报,但赛季中缺少"我的里程碑"总览。
   改动:在 MyCard 增加「📜 生涯成就」页签,汇总:历史总得分/篮板/助攻、MVP/DPOY/得分王/最佳阵容次数、冠军数、全明星次数、连冠、生涯之夜次数、GOAT评分等,一眼看清自己在哪。

8. **更多商店道具 / 事件**(可选,依赖工作量)
   - 商店增加「状态型道具」:如「体能恢复包」「关键球挂饰」等一次性消耗品。
   - 事件池增加少量新球场地缘事件(如「全明星周末趣事」「季后赛更衣室冲突」)。

### D. 数值 / 机制细节(只在你指定的前提下动)

9. 尊重原版 — 默认**不改动** `game-config.js` 中已被作者 `★` 标注精心调校的数值(命中率、数据缩放、MVP 概率、工资帽等)。
   若你后续想调某一项(如"三分更准""夺冠更易""生涯更长"),指出来我单独改,并在文档记录改动点以便回退。

## 三、改动风险与回退策略

- 所有改动在 `game-config.js` 通过**新增配置字段+难度乘数**实现,原数值保留为"标准档"默认值。
- UI 改动均为**新增按钮/页签**,不删原功能。
- 每次改动前用 `undo_snapshot` 留存快照;若效果不佳可 `undo_restore` 回退。
- 修改后本地 `server.ps1`(http://localhost:8765)验证可正常运行、无 JS 报错、能走通一整个赛季。

## 四、交付前验证清单

- [ ] 本地静态服务器启动,页面正常加载
- [ ] 创建球员 → 选时代 → 进入赛季,全流程无 JS 报错
- [ ] 快进整季 + 自动暂停中断点(事件/伤病/决胜球)正常弹窗
- [ ] 三档难度切换后模拟参数生效
- [ ] 新历史时代可选并可开局
- [ ] 成就页签数据汇总正确
- [ ] 存档/读档后新功能状态保留
